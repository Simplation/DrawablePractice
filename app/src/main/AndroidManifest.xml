<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.moudle.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestRoundActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ① BitmapDrawable -->
        <activity android:name=".BitmapDrawableActivity"/>

        <!-- ② LayerDrawable -->
        <activity android:name=".LayerDrawableActivity"/>

        <!-- ③ StateDrawable -->
        <activity android:name=".StateDrawableActivity"/>

        <!-- ④ LevelDrawable -->
        <activity android:name=".LevelDrawableActivity"/>

        <!-- ⑤ TranslationDrawable -->
        <activity android:name=".TranslationDrawableActivity"/>

        <!-- ⑥ InsetDrawable -->
        <activity android:name=".InsetDrawableActivity"/>
        <!-- ⑦ ClipDrawable -->
        <activity android:name=".ClipDrawableActivity" />

        <!--测试圆角图片-->
        <activity android:name=".TestCornerActivity" />
    </application>

</manifest>